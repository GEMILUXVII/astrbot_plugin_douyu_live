# 更新日志

本文件记录 `astrbot_plugin_douyu_live` 插件的版本更新历史。

## [2.2.5] - 2026-07-27

### 修复

- 依赖升级至 `aiodouyu>=0.4.4,<0.5`；重载继承的待定状态在应用前强制
  重新 HTTP 对账，避免旧快照在新监控完成首次对账前触发状态通知

## [2.2.4] - 2026-07-27

### 修复

- 依赖升级至 `aiodouyu>=0.4.3,<0.5`，真实下播须经 90 秒稳定复核，
  并补偿斗鱼 HTTP 状态更新延迟，避免假下播和漏开播
- 依赖库监控日志接入 AstrBot，恢复启动、连接、状态确认与停止日志
- 平台暂未就绪时不再把通知误判为成功；失败通知持续退避重试至 6 小时
- 插件重载会保存正在发送或等待重试的通知，并在启动后继续发送，避免
  监控状态已继承但通知队列被取消所造成的永久漏报
- 补充通知入队、发送尝试和发送成功日志，能明确区分状态识别与平台投递

## [2.2.3] - 2026-07-27

### 修复

- 状态监控下沉至 `aiodouyu.LiveStatusMonitor`：所有原始 `rss` 转换都须经
  betard HTTP 对账确认，避免不完整消息、状态抖动或冲突快照产生假下播
- 插件移除重复的弹幕连接、状态解析、对账重试及冷却状态机，仅保留
  AstrBot 通知与生命周期适配
- 依赖范围同步至 `aiodouyu>=0.4.2,<0.5`
- 移除锁定旧版 `aiodouyu 0.2.0` 的过期 `uv.lock`；AstrBot 继续按
  `requirements.txt` 从 PyPI 安装依赖

## [2.2.2] - 2026-07-27

### 变更

- **通知与命令回复改为纯文本排版**：移除开播/下播通知与命令回复里的
  装饰性 emoji 及分隔线，改用 `【标题】` + `字段: 值` 的正式格式
  （正式推送场景下 emoji 显得不专业）
  - 保留 `/douyu ls` 的 🟢 运行中 / 🟡 重连中 / 🔴 已停止 状态色块，
    它们承载状态语义而非装饰
  - `✅/❌` 类前缀改为 `注意:` / `失败:` 或直接去掉；`@全体` 开关显示
    改为 `开/关`
  - 富化字段（标题/分类）与封面图行为不变；封面图与文本本就在同一条
    消息里发送（同一消息链：`@全体 + 文本 + 图片`）

## [2.2.1] - 2026-07-27

适配版:依赖抬至 aiodouyu 0.2(`>=0.2.0,<0.3`)。0.2 带来 typed models
与公开测试基建(`aiodouyu.testing`),插件运行时行为不变;后续版本将
逐步把测试夹具迁移到 `aiodouyu.testing` 并用 `replay()` 喂真实语料做
回归。

## [2.2.0] - 2026-07-27

功能版:六件套 + 外呼防护地基。全部设计经宿主源码验证
(裸 /douyu 组名由框架回复命令树、`_conf_schema` options 写法、
GreedyStr 约束、qq_official 限制均已规避)。

### 新增

- **WebUI 插件配置**(`_conf_schema.json`):通知富化/封面图开关、
  重启补播报开关(`catchup_announce`)、通知冷却秒数、订阅权限档位
  (everyone/admin,运行时检查)、场次保留天数
- **开播通知富化**:附带直播间标题、分类与封面图(经 TTL 缓存 +
  并发限制的外呼,失败自动降级为基础文本;重试时先弃封面再弃 @全体)。
  消息构建移到队列协程侧,监控回调零阻塞
- **/douyu live**:查看当前在播房间(在播判定用内存状态机,HTTP 只做
  富化,单房失败降级显示)
- **/douyu help**:命令帮助,管理员可见管理段
- **/douyu offline <房间号> [on/off]**:按群关闭下播通知(默认开,
  旧订阅自动视为开)
- **场次历史落盘**:开播/下播事件按房间落 JSONL(`sessions/{rid}.jsonl`,
  含标题/分类/时长),按配置天数自动清理;为 /douyu stats、周报铺路
- **干净重启不再补播报**:关停时保存监控状态快照,启动一次性回灌
  (崩溃重启无快照时按 `catchup_announce` 配置决定是否补发;快照
  超 24 小时按无效处理)
- **通知时间显式北京时间**:Docker 默认 UTC 部署不再偏 8 小时
- 金丝雀 CI:每周对 AstrBot master 探测内部 API 与跑插件测试

## [2.1.3] - 2026-07-27

第四轮审计(2000 组随机序列模型仿真零违例)后的小修版。

### 修复

- **依赖下界抬至 `aiodouyu>=0.1.2`**:此前下界 0.1.0 会让升级用户被宿主
  "依赖已满足"跳过而滞留旧库,与 CHANGELOG"配套 0.1.x"的声明脱节
- **下播时长不再含冷却补发延迟**:冷却期记录的下播转换补发时,时长终点
  改为观测时刻(此前按补发时刻计,单场虚增最多约 30-40 秒);开播方向
  同步用观测时刻兜底
- 文档同步:FAQ 明确"宿主重启补发开播通知是有意行为"(此前引导用户
  误报 bug)、补 🟢/🟡/🔴 状态图例与断连补偿/退避重试说明、徽章更新
- 测试清理:移除 `asyncio.get_event_loop()` 弃用形态

## [2.1.2] - 2026-07-27

第三轮适配审计(实证复现 + 对抗验证)后的修复版。配套 aiodouyu 0.1.2。

### 修复

- **校准 tick 内执行顺序(中危)**:过期的抖动 pending 此前先于到期对账
  补发,重连恰落在 pending 过期同一 tick(或重启交接继承已过期 pending)
  时,一场未中断的直播会收到"虚假下播 + 重复开播"通知对。现在对账先行,
  其快照经"回稳"分支清除抖动 pending
- **并发保存洗白(中危,失败注入实测复现)**:新快照写盘失败后,排队中的
  更旧快照成功落盘会把"保存失败"状态洗白,用户失去磁盘缺失最新变更的
  警告。现以失败序列号水位防护
- 重连不再把"房间不存在"的 30 分钟复查排程重置为立即(空闲超时重连循环
  此前会把复查频率打回分钟级)
- subscribe 在数据层锁内判存,根治与删房间并发时重建孤立订阅的 TOCTOU
- 损坏的备份文件同样隔离(不禁写),不再被后续保存轮转静默覆盖
- 通知发送加 30 秒纵深防御超时,挂死的第三方适配器按失败目标走退避重试
- 补上 `_write_blocked` 最后防线的回归测试覆盖

## [2.1.1] - 2026-07-27

适配审计(多智能体对抗验证)后的修复版。配套 aiodouyu 0.1.1。

### 修复

- **对账过期快照竞态(中危)**:慢速 HTTP 对账在途期间状态机若已前进
  (收到 rss)或连接已更替,带回的过期快照可能覆盖较新状态、记出虚假的
  反向待定转换(补发假通知 + 吞掉真实转换)。对账改为仅由校准协程串行
  执行(消费循环只登记请求,不再被 HTTP 阻塞),并以观测序号 + 连接代际
  做新鲜度校验,过期快照直接丢弃重拉
- **对账重试计数溢出(中危)**:连续失败约 17 小时后指数退避计算抛
  OverflowError,静默杀死校准协程(冷却补发与对账全部失效且无任何告警)。
  指数已钳制,校准协程循环体增加异常兜底
- **依赖上界(中危)**:锁定 `aiodouyu>=0.1.0,<0.2`,防止未来库的 0.x
  破坏性发布打断新安装
- 封禁/删除的房间不再以 60 秒间隔无限重试对账:识别为确定性结论后改为
  每 30 分钟复查,状态变化时只告警一次
- `_reconcile_pending` 补上 stop 检查,停止窗口内不再可能触发回调
- `stop()` 等待消费协程改用 `asyncio.wait`,消除 shield 对"任务被第三方
  取消"与"stop 自身被取消"的误判

## [2.1.0] - 2026-07-26

### 变更（Breaking：依赖更换）

- **弹幕客户端从 pydouyu 迁移到自研 [aiodouyu](https://github.com/GEMILUXVII/aiodouyu)**：
  监控器从"每房间一线程 + 跨线程回调"重写为单协程 asyncio 模型
  - 移除依赖 `pydouyu`、`httpx`，新增 `aiodouyu>=0.1.0`（零传递依赖）
  - 删除 `core/api.py`，房间信息改用 `aiodouyu.fetch_room()`（betard 优先、
    公开 API 兜底，可区分"房间不存在"与"接口暂时不可用"）
  - 断线重连、半开连接检测由库内置（指数退避 + 空闲超时），监控代码大幅简化

### 修复

- **断连窗口丢通知（高危）**：斗鱼只在状态变化时推送 rss，此前 pydouyu 静默
  内部重连期间发生的开播/下播永久丢失，且后续反向事件会被状态机"回稳"分支吞掉，
  整场直播无任何通知。现在每次弹幕连接建立（含自动重连）后都用 HTTP 接口对账
  当前开播状态并喂入状态机，补齐窗口内丢失的转换；对账同时修正跨断连的直播时长。
  对账失败按 5/10/20/40/60s 退避自动重试直到成功，不因接口偶发故障留下缺口
- **通知重试形同虚设**：重试间隔此前仅 0.5 秒（5 次重试约 2.5 秒内耗尽），
  扛不住真实的平台抖动；现按 5/15/45/120 秒指数退避，覆盖分钟级故障
- **数据文件畸形键崩溃**：`str.isdigit()` 对 "²"/"①" 等 Unicode 数字字符返回
  True 但 `int()` 抛异常，手工编辑过的数据文件会让插件加载直接崩溃，现限定 ASCII
- **文本清洗绕过**：主播昵称清洗补齐 bidi isolate 控制符 U+2066-2069
  （Trojan-Source 载体）、方向标记 U+200E/200F/U+061C 与零宽字符，
  防止外部可控昵称视觉重排通知内容
- **去重时序**：通知去重时间戳改为入队成功后记录，队满丢弃时不再误吸收
  窗口内的同事件补发
- **重启交接丢状态**：重启监控时状态快照改为旧监控完全停止后导出，
  交接窗口内的状态转换（含冷却期待定转换）不再随旧监控消亡

### 其他

- `/douyu ls` 新增 "🟡 重连中" 状态：弹幕连接长期不通（库在退避重连）时
  如实展示，不再被 "🟢 运行中" 掩盖
- 冷却期补发的开播转换保留观测源的开播时间，直播时长不再包含冷却延迟
- 行为说明：1.3.0 起"宿主重启后对进行中的直播补发开播通知"是有意特性；
  迁移后该行为由"偶发"（取决于斗鱼是否恰好推送）变为**必现**（重启后
  对账必然检测到在播状态）。后续版本将随场次历史落盘改为可配置
- 移除 `uv.lock`：aiodouyu 发布到 PyPI 后重新生成（`uv lock`）

## [2.0.0] - 2026-07-26

### 移除（Breaking）

- **礼物播报功能整体移除**：包括 `/douyu gift`、`/douyu giftfilter` 命令、
  订阅配置中的 `gift_notify` / `high_value_only` 字段、礼物 ID 映射表与礼物播报限流
  - 旧数据文件中的礼物相关字段在加载时自动忽略，下次保存时清除，无需手动迁移
  - 移除后监控只处理 `rss`（直播状态）消息，不再订阅 `dgb`（礼物）消息

### 修复

- **数据安全**：数据文件改为原子写入（临时文件 + fsync + 原子替换），并自动保留上一代备份（`.bak`）
  - 加载失败不再清空数据回写：损坏的主文件会被隔离为 `.corrupt.<时间戳>` 文件，并尝试从备份恢复
  - 单个非法条目（如手工编辑引入的坏键）只跳过自身，不再导致整个数据文件加载失败
  - 数据保存失败时会在命令回复中提示，不再静默假装成功
- **通知正确性**：修复 rss 冷却期吞掉真实状态转换导致"漏发下播通知 + 漏发下一场开播通知"的问题
  - 冷却期内的状态变化改为记录待定状态，冷却结束后自动校准补发
- **重启不再重复播报**：`/douyu restart` 与 watchdog 重启监控时继承旧监控的直播状态，不再对正在直播的房间重发开播通知
- **事件循环卡顿**：停止/重启/删除监控时的阻塞等待（最长 5 秒/房间）移至线程池执行，不再冻结整个 bot
- 监控启动失败现在能真实上报（此前 `start()` 恒返回成功，失败分支均为死代码）
- `send_message` 返回 `False`（无匹配平台）不再被记为发送成功；发送失败的目标会按目标粒度重试（最多 5 轮），不再对已成功目标重复发送
- 添加房间时校验修复：斗鱼对不存在的房间可能返回 200 + 空数据，此前会被误判为有效房间
- 监控线程内锁外访问 `client` 的竞态、`join` 超时被忽略产生僵尸线程等问题
- **监控生命周期串行化**：watchdog 与管理员命令并发重启/删除同一房间时可能泄漏运行中的监控器并产生双份通知，现按房间加锁并在操作后复查房间存在性
- **停止窗口竞态**：`stop()` 恰在监控线程启动客户端前插入时，pydouyu 客户端可能被反向启动并永久泄漏连接，现启动后复查停止标志
- **待定状态继承**：重启监控时冷却期内记录的待定状态转换随状态一并继承，不再被静默丢弃（否则漏发下播 + 漏发下一场开播）
- **数据文件形状校验**：合法 JSON 但顶层非对象（如 `[]`/`null`）的主文件同样走隔离 + 备份回退，不再绕过保护并在下次保存时覆盖完好备份
- 同一房间同类通知增加 10 秒去重窗口，吸收重启时新旧监控重叠产生的重复播报
- 磁盘写入（含 fsync）移至线程池执行且不占用数据锁，命令处理不再阻塞事件循环
- 退订历史仅归档非默认配置且每房间限 100 条，不再无界增长
- `/douyu add` 的名称参数支持包含空格（此前多词名称被静默截断为第一个词）
- 开关命令收到无法识别的参数时报错，不再静默执行切换（避免与用户预期相反）
- `remove_room` 对仅存在于退订历史的房间不再出现内存已改但未落盘的不一致
- release 工作流的 tag 输入改经环境变量传递并校验格式，消除表达式注入面
- 清洗正则补充 C1 控制字符（含 U+0085）与双向文本覆盖符

### 新增

- **监控看门狗**：每 60 秒检测失效的监控（连接断开/线程退出）并自动重启
- **退订保护**：取消订阅时保留该群的订阅配置，重新订阅时自动恢复；订阅/退订操作记录操作者 ID 以便审计
- 来自斗鱼接口的主播/房间名称等外部文本在进入通知与命令回复前会被清洗（移除换行/控制字符、归一化框线字符、截断），防止伪造通知内容
- 房间号入参校验（必须为正整数）；斗鱼 API 请求增加 User-Agent

### 工程

- 新增 `tests/` pytest 套件（32 个用例，含 AstrBot/pydouyu 桩），覆盖状态机、
  持久化容错、数据迁移、通知队列重试与去重等全部核心修复的回归防线
- 新增 CI 工作流（push/PR 触发 ruff + pytest，py3.10/3.12 矩阵，依赖声明一致性校验）
- ruff 规则固化进插件 pyproject.toml，不再隐式依赖宿主 AstrBot 的配置
- release 工作流新增 tag 与 metadata.yaml / pyproject.toml 版本一致性断言
- metadata.yaml 补充 `display_name`、`short_desc`、`astrbot_version`（>=4.25,<5，
  版本闸门拦截不兼容宿主）
- LOGO.png（7MB）压缩为 logo.png（39KB）：修复 Linux 下因文件名大小写不符
  导致 WebUI 不显示图标的问题，并大幅缩小安装包体积

### 变更

- 通知发送统一经由队列串行调度：保证发送顺序、限制并发（单条通知扇出并发 5）
- `/douyu ls`、`/douyu status`、`/douyu sub` 的监控状态判定统一为真实线程存活状态，不再互相矛盾
- 每次成功发送的日志级别由 INFO 降为 DEBUG，减少刷屏
- `RoomInfo` 移除已失效的房间级通知设置字段（1.4.0 起已由订阅级配置取代；旧数据迁移不受影响）
- 依赖锁定：`pydouyu==0.0.7`
- 清理死代码（未使用的 EMOJI 常量、`get_streamer_name`、`update_room` 等）；`core/api.py` 的 `RoomInfo` 更名为 `DouyuRoomData` 以避免与数据模型重名
- Release 包不再包含 LOGO.png 与 uv.lock

---

## [1.4.1] - 2025-12-30

### 修复

- 修复 `pydouyu` 导入异常被捕获导致 AstrBot 无法自动安装依赖的问题（[#1](https://github.com/GEMILUXVII/astrbot_plugin_douyu_live/issues/1)）
  - 移除对 `pydouyu` 导入的 try-except 处理
  - 当依赖缺失时，插件导入会失败，AstrBot 将自动读取 `requirements.txt` 并安装依赖

### 变更

- 移除 `PYDOUYU_AVAILABLE` 全局变量及相关检查逻辑
- 更新 README.md，移除手动安装依赖的步骤说明

---

## [1.4.0] - 2025-12-10

### 新增

- **订阅级别设置隔离**：`@全体成员`、`礼物播报`、`高价值过滤` 设置现在按群独立配置
  - 每个群可以对同一直播间设置不同的配置，互不影响
  - 新增 `SubscriptionConfig` 数据模型存储每个群的独立设置
- `/douyu mysub` 命令现显示当前群的订阅配置状态

### 变更

- `/douyu atall`、`/douyu gift`、`/douyu giftfilter` 命令现在只影响当前群的订阅
  - 执行这些命令前需要先在当前群订阅直播间
- `/douyu ls` 命令不再显示房间级别的设置（因设置现为订阅级别）
- 数据结构从 `dict[int, set[str]]` 改为 `dict[int, dict[str, SubscriptionConfig]]`

### 迁移

- 旧版数据格式会在首次加载时自动迁移
- 迁移时订阅配置会继承原房间级别的设置

---

## [1.3.0] - 2025-12-02

### 新增

- 新增 **下播通知**，自动计算本次直播时长并推送至订阅源
- 插件启动后若检测到直播已进行，立即推送一次开播通知
- 新增 GitHub Actions 工作流，依据 `v*` 标签自动打包并发布 Release

### 优化

- `DouyuMonitor` 新增状态冷却与一次性下播保护，解决开播即误判下播的问题
- `DouyuMonitor` 支持在冷却期内保持原状态，避免漏发真实下播通知
- `Notifier` 新增发送失败自动重试，并在重试时自动降级 @全体，降低风控概率

### 修复

- 修复 pydouyu 连接抖动导致的重复通知/漏通知问题
- 修复 NapCat 接口短暂失败时通知直接丢失的问题

---

## [1.2.2] - 2025-12-02

### 修复

- 修复数据加载时旧版本字段兼容性问题（`min_gift_price` -> `high_value_only`）
- 修复 pydouyu Socket 连接失败后无法恢复的问题
- 优化监控器连接逻辑，支持断线自动重连
- 修复插件重载时 Socket 状态异常的问题

### 变更

- `DouyuMonitor` 延迟创建 Client，避免初始化时的 Socket 问题
- 添加 `_stop_flag` 标志，实现优雅的线程停止
- 连接断开后 10 秒自动重连

---

## [1.2.1] - 2025-12-02

### 新增

- 新增 `/douyu giftfilter <房间号> [on/off]` 命令，可选择是否只播报高价值礼物
- 高价值礼物过滤默认开启，只播报飞机及以上的礼物

### 变更

- `RoomInfo` 数据模型将 `min_gift_price` 改为 `high_value_only` 布尔字段
- `/douyu ls` 命令现显示礼物过滤模式（仅高价值/全部）
- `/douyu gift` 命令现同时显示过滤模式状态

### 修复

- 修复礼物映射表中重复的键值
- 修复代码风格问题（ruff W293）

---

## [1.2.0] - 2025-12-02

### 新增

- **礼物播报功能**：直播间收到礼物时自动在订阅群内播报
  - 新增 `/douyu gift <房间号> [on/off]` 命令，管理员可开启/关闭礼物播报
  - 支持常见斗鱼礼物名称显示（粉丝荧光棒、飞机、火箭等）
  - 未知礼物显示为 "神秘礼物(ID)"
- 新增 `utils/constants.py` 礼物 ID 到名称的映射表

### 变更

- `RoomInfo` 数据模型新增 `gift_notify` 和 `min_gift_price` 字段
- `DouyuMonitor` 现支持 `live_callback` 和 `gift_callback` 双回调
- `Notifier` 新增 `build_gift_notification()` 方法
- `/douyu ls` 命令现显示礼物播报状态

---

## [1.1.0] - 2025-12-02

### 新增

- **项目模块化重构**：
  - `core/monitor.py` - 斗鱼监控器封装
  - `core/api.py` - 斗鱼 API 调用（自动获取主播名称）
  - `core/notifier.py` - 通知构建与发送
  - `storage/data_manager.py` - 数据持久化管理
  - `models/room.py` - 房间信息数据模型
  - `utils/constants.py` - 常量定义
- **自动获取主播名称**：添加房间时若未指定名称，自动从斗鱼 API 获取

### 依赖

- 新增 `httpx>=0.24.0` 用于异步 HTTP 请求

---

## [1.0.0] - 2025-11-28

### 初始版本

- 斗鱼直播开播通知功能
- 多房间监控支持
- 群订阅机制
- @全体成员通知选项
- 管理员权限控制

### 命令

- `/douyu add <房间号> [名称]` - 添加监控
- `/douyu del <房间号>` - 删除监控
- `/douyu ls` - 查看监控列表
- `/douyu sub <房间号>` - 订阅通知
- `/douyu unsub <房间号>` - 取消订阅
- `/douyu mysub` - 查看我的订阅
- `/douyu status` - 查看监控状态
- `/douyu restart [房间号]` - 重启监控
- `/douyu atall <房间号> [on/off]` - 设置@全体
